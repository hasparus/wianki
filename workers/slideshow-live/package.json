{
	"name": "@wedding/slideshow-live",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"check": "npm run typecheck && npm run test"
	},
	"dependencies": {
		"jose": "^6.1.0",
		"partyserver": "^0.5.10"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250917.0",
		"typescript": "^5",
		"vitest": "^3.2.4",
		"wrangler": "^4.38.0"
	}
}
